# ROADMAP — 运行本的完整形态

> 原则：**一个视图组件，多布局；全部数据源零 API；交互层永不因视图切换丢失。**

## 架构分层（当前状态）

| 层 | 组成 | 状态 |
|---|---|---|
| L1 数据源 | ① 会话时间线 ② git 提交（cwd 账本）③ 子会话日志（zstd 挖掘）④ 磁盘扫描 + 静态 IO 推断 ⑤ PIPELINE.md 人工覆盖 | ✅ |
| L2 图构建 | `buildFileGraph`：runs 骨架 → 扫描文件 → 静态 IO 边 → 回显噪声（限流）→ git 提交节点/链 → 子 agent 子图 → 账本补边；环防护 | ✅ |
| L3 布局 | `layoutFileGraph`（会话流：rank 分层 + 搁架）/ `layoutBackbone`（主链：拓扑分层 + 阶段卡片 + 挂靠 + 幽灵）——**同一输出契约** | ✅ |
| L4 交互 | 滑杆回放 / 悬停 👁▶✨ / 检查器 / 解释竞速（主模型×glm 并发）/ 缩放 detent / fit / 图例 / 搁架收纳 / 目录覆盖（localStorage 持久） | ✅ |

## 版本史

- v0.1.0 会话 DAG + git 提交节点 + 子 agent 交接 + 持久账本
- v0.2.0 磁盘扫描 + 三层精度静态 IO（未运行/未推送代码进图）
- v0.3.0 PIPELINE.md 骨架（文档即结构，缺口幽灵）
- v0.4.0 语义徽章 + 目录覆盖 + 零 API 推断主链
- v0.5.0 **统一视图**：骨架成为布局而非独立组件，全部交互保留；目录覆盖持久化

## 下一步（按价值排序）

- [ ] **提交节点 git show**：阶段/提交节点上直接看 diff（paperlab 式"改-验-交"）
- [ ] **文件代际**：同一文件重写 N 次 → N 节点连线（dsh-science 式溯源）
- [ ] **骨架双向编辑**：推断主链可一键导出为 PIPELINE.md（人工校对后固化）
- [ ] **全局会话总结**：一段话说明整个会话干了什么（唯一计划中的 API 调用，可选）
- [ ] 大图性能：>300 节点虚拟化 / 边聚合

## 设计决策记录

- PIPELINE.md 是**可选覆盖**不是依赖——主链可从静态 IO 零成本推断，文档只在人工想钉死结构时写
- 解释用并发竞速而非串行 fallback：主模型常空响应，glm 长 prompt 30s，竞速 2–6s 出结果
- 孤儿文件默认收起：主流水线永远是视口主角，背景按目录分组收纳
